{
  "name": "my-monorepo",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/node": "^26.0.1",
    "concurrently": "^9.2.4",
    "ts-node-dev": "^2.0.0",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  },
  "scripts": {
    "dev:server": "npm run dev --workspace=server",
    "dev:client": "npm run dev --workspace=client",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "db:migrate": "export $(cat server/.env | xargs) && npx prisma migrate dev --schema=server/prisma/schema.prisma",
    "db:seed": "export $(cat server/.env | xargs) && npx prisma db seed",
    "db:studio": "export $(cat server/.env | xargs) && npx prisma studio --schema=server/prisma/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3",
    "bcrypt": "^6.0.0"
  },
  "prisma": {
    "seed": "npx tsx server/prisma/seed.ts"
  }
}
